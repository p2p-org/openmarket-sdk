// tslint:disable:no-expression-statement
import test from 'ava'
// import WebSocket from 'ws'
// import { DGMarketAPI } from './api'

// global.WebSocket = WebSocket

test(`DGMarketAPI`, async (t) => {
  // const api = new DGMarketAPI({})
  //
  // t.is(api.version(), 'v1')
  // t.is(await api.getOneNft('123123'), null)
  t.is(true, true)
  // it(`should show v1 and print deprecation message if in dev mode`, () => {
  //   const spyWarn = jest.spyOn(console, 'warn')
  //   const actual = api.apiVersion()
  //   const expected = 'v1'
  //
  //   expect(actual).toBe(expected)
  //   expect(spyWarn).toHaveBeenCalledWith('this method is deprecated, use #version instead')
  // })

  // it(`should getAllNft returns array`, () => {
  //   expect.assertions(1)
  //
  //   return api.getAllNft().then((data) => expect(Array.isArray(data)).toBe(true))
  // })
  //
  // it(`should getOneNft non existent returns null`, () => {
  //   expect.assertions(1)
  //
  //   return api.getOneNft('123123').then((data) => expect(data).toBe(null))
  // })
  // it(`should getOneNft returns array`, () => {
  //   expect.assertions(1)
  //
  //   return api.getOneNft('E23152C5-3D33-4B1D-8285-80F11415DDDA').then((data) => expect(typeof data).toBe('object'))
  // })
})
